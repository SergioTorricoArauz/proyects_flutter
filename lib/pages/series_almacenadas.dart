import 'package:buscador_de_peliculas/models/series.dart';
import 'package:flutter/material.dart';

class SeriesAlmacenadas extends StatelessWidget {
  const SeriesAlmacenadas(
      {super.key,
      required List<Series> peliculas,
      required List<Series> peliculaService});

  @override
  Widget build(BuildContext context) {
    throw UnimplementedError();
  }
}
